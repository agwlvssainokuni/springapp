<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cherry.spring.common.db.app.mapper.SignupRequestMapper2">
	<select id="validateMailAddr" resultType="boolean">
		SELECT
			CASE
				WHEN COUNT(*) = 0 THEN 1
				WHEN COUNT(*) >= #{numOfReq,jdbcType=INTEGER} THEN 0
				WHEN MAX(A.applied_at) >= DATEADD('SECOND', - #{intervalInSec,jdbcType=INTEGER}, CURRENT_TIMESTAMP) THEN 0
				ELSE 1
			END
		FROM
			signup_request AS A
		WHERE
			A.mail_addr = #{mailAddr,jdbcType=VARCHAR}
			AND
			A.applied_at >= DATEADD('SECOND', - #{rangeInSec,jdbcType=INTEGER}, CURRENT_TIMESTAMP)
			AND
			A.deleted_flg = 0
	</select>
	<select id="validateToken" resultType="boolean">
		SELECT
			COUNT(*)
		FROM
			signup_request AS A
		WHERE
			A.mail_addr = #{mailAddr,jdbcType=VARCHAR}
			AND
			A.token = #{token,jdbcType=VARCHAR}
			AND
			A.applied_at >= DATEADD('SECOND', - #{validInSec,jdbcType=INTEGER}, CURRENT_TIMESTAMP)
			AND
			A.deleted_flg = 0
			AND
			NOT EXISTS (
				SELECT * FROM signup_request AS B
				WHERE
					B.mail_addr = A.mail_addr
					AND
					B.applied_at > A.applied_at
					AND
					B.deleted_flg = 0
			)
	</select>
</mapper>
